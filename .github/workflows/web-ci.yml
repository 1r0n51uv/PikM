name: web-ci

on:
  pull_request:
    paths:
      - "apps/web/**"
      - "packages/shared/**"
      - "pnpm-workspace.yaml"
      - "package.json"
      - "turbo.json"
      - ".github/workflows/web-ci.yml"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm --filter @1r0/shared typecheck
      - run: pnpm --filter @1r0/web lint
      - run: pnpm --filter @1r0/web typecheck
      - run: pnpm --filter @1r0/web build
